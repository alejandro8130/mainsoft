<div class="container">
  <div class="row">
    <h1 class="text-center texto-formularios"><span class="borde-titulo-form">Departamentos</span></h1>
    <br>
    <div class="col-md-offset-8">
      <div class="well well-sm">
        <%= form_tag departamentos_path, class: "form-inline", role: "form",  method: 'get' do %>
  	      <label>Nombre: </label>
  	      <div class="form-group">
     	      <%= search_field_tag :search, params[:search], :class => "form-control", placeholder: "Ingrese nombre para Buscar", size: "25" %>
  	      </div>
  	      <button class="btn btn-primary btn-sm">
      	    <%= image_tag("search.png") %>
  	      </button>
        <% end %>
      </div>
    </div>
    
    <hr>
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th>Nombre</th>
            <th colspan="3"></th>
          </tr>
        </thead>
      
        <tbody>
          <% @departamentos.each do |departamento| %>
            <tr>
              <td><%= departamento.nombre %></td>
              <td><%= link_to 'Mostrar', departamento, class:"btn boton-azul" %></td>
              <td><%= link_to 'Editar', edit_departamento_path(departamento), class:"btn boton-amarillo" %></td>
              <td><%= link_to 'Eliminar', departamento, method: :delete, data: { confirm: 'Está Seguro?' }, class:"btn boton-rojo" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      
      <br>
      <%= will_paginate @departamentos, renderer: BootstrapPagination::Rails %>



      <br>
      Departamentos: <%= @departamentos.count %>
      <br>

    </div>  <%= link_to 'Nuevo Departamento', new_departamento_path, class:"btn boton-verde" %>
  </div>
</div>